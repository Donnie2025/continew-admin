# 会员卡类型字典配置说明

## 问题描述
会员卡管理功能需要以下四种类型的字典数据：
- **TL**: 次卡有限期 - 有固定次数和有效期限制
- **TU**: 次卡无限期 - 有固定次数但无有效期限制  
- **BL**: 储蓄卡有限期 - 有余额和有效期限制
- **BU**: 储蓄卡无限期 - 有余额但无有效期限制

## 解决方案

### 方案一：执行SQL脚本（推荐）

1. 连接到数据库 `continew_admin`
2. 执行以下SQL脚本：

```sql
-- 添加会员卡类型字典
INSERT IGNORE INTO sys_dict (name, code, description, is_system, create_user, create_time)
VALUES ('会员卡类型', 'card_type', '会员卡类型字典', 1, 1, NOW());

-- 获取字典ID
SET @dict_id = (SELECT id FROM sys_dict WHERE code = 'card_type');

-- 添加字典项
INSERT IGNORE INTO sys_dict_item (label, value, color, sort, description, status, dict_id, create_user, create_time)
VALUES 
('次卡有限期', 'TL', 'blue', 1, '次卡有限期：有固定次数和有效期限制', 1, @dict_id, 1, NOW()),
('次卡无限期', 'TU', 'green', 2, '次卡无限期：有固定次数但无有效期限制', 1, @dict_id, 1, NOW()),
('储蓄卡有限期', 'BL', 'orange', 3, '储蓄卡有限期：有余额和有效期限制', 1, @dict_id, 1, NOW()),
('储蓄卡无限期', 'BU', 'purple', 4, '储蓄卡无限期：有余额但无有效期限制', 1, @dict_id, 1, NOW());
```

### 方案二：通过管理后台添加

1. 登录管理后台
2. 进入 **系统管理** -> **字典管理**
3. 添加字典：
   - 字典名称：会员卡类型
   - 字典编码：card_type
   - 字典描述：会员卡类型字典
   - 是否系统：是

4. 添加字典项：
   | 标签 | 值 | 颜色 | 排序 | 描述 |
   |------|----|----|------|------|
   | 次卡有限期 | TL | blue | 1 | 次卡有限期：有固定次数和有效期限制 |
   | 次卡无限期 | TU | green | 2 | 次卡无限期：有固定次数但无有效期限制 |
   | 储蓄卡有限期 | BL | orange | 3 | 储蓄卡有限期：有余额和有效期限制 |
   | 储蓄卡无限期 | BU | purple | 4 | 储蓄卡无限期：有余额但无有效期限制 |

## 验证方法

执行以下SQL验证字典是否添加成功：

```sql
SELECT 
    d.name as dict_name,
    d.code as dict_code,
    di.label,
    di.value,
    di.color,
    di.sort,
    di.description
FROM sys_dict d 
LEFT JOIN sys_dict_item di ON d.id = di.dict_id 
WHERE d.code = 'card_type' 
ORDER BY di.sort;
```

## 前端使用

前端代码已经正确配置，会自动使用 `card_type` 字典：

```typescript
// 在组件中使用
const { card_type } = useDict('card_type')

// 在模板中使用
<GiCellTag :value="record.type" :dict="card_type" />
```

## 注意事项

1. 字典编码必须是 `card_type`
2. 字典项的值必须是 `TL`, `TU`, `BL`, `BU`
3. 添加字典后需要清除缓存或重启应用
4. 确保字典项的状态为启用状态（status = 1）

## 文件位置

- 主数据文件：`continew-webapi/src/main/resources/db/changelog/mysql/main_data.sql`
- 测试脚本：`test_card_type_dict.sql`
- 独立脚本：`add_card_type_dict.sql`
