# 部署脚本使用指南

## 📋 更新说明

已更新 `deploy.sh` 脚本，新增了 **continew-common-3.7.0-SNAPSHOT.jar** 的上传。

这个模块包含了 `UserContext` 类的更新（新增了 `openid` 字段），必须部署才能解决 `NoSuchMethodError` 问题。

---

## 🚀 使用方法

### 1. 编译项目

```bash
cd /Users/wangdong/Workspace/Young/continew-admin

# 清理并编译
mvn clean package -DskipTests
```

### 2. 运行部署脚本

```bash
cd /Users/wangdong/Workspace/Young/continew-admin

# 运行部署脚本
./deploy.sh
```

脚本会自动：
- ✅ 检查所有必需的文件是否存在
- ✅ 测试SSH连接
- ✅ 创建远程目录结构
- ✅ 上传以下文件：
  - 主程序 JAR
  - **continew-common 模块** （包含 UserContext 更新）
  - continew-module-system 模块
  - continew-module-education 模块
  - 配置文件
  - 证书文件

### 3. 登录服务器重启服务

```bash
# SSH连接到服务器
ssh tx

# 进入项目目录
cd /www/wwwroot/continew-admin

# 重启服务
./restart.sh

# 或手动重启
kill -9 $(lsof -ti:8080)
nohup java -jar bin/continew-admin.jar > app.log 2>&1 &

# 查看日志
tail -f app.log
```

---

## 📦 上传的文件清单

| 文件 | 本地路径 | 远程路径 | 说明 |
|------|---------|---------|------|
| 主程序 | `continew-webapi/target/app/bin/continew-admin.jar` | `/www/wwwroot/continew-admin/bin/` | 主应用程序 |
| **Common模块** | `continew-webapi/target/app/lib/continew-common-3.7.0-SNAPSHOT.jar` | `/www/wwwroot/continew-admin/lib/` | **包含UserContext更新** |
| System模块 | `continew-webapi/target/app/lib/continew-module-system-3.7.0-SNAPSHOT.jar` | `/www/wwwroot/continew-admin/lib/` | 系统模块 |
| Education模块 | `continew-webapi/target/app/lib/continew-module-education-3.7.0-SNAPSHOT.jar` | `/www/wwwroot/continew-admin/lib/` | 教育模块 |
| 配置文件 | `continew-webapi/src/main/resources/config/*` | `/www/wwwroot/continew-admin/config/` | 应用配置 |
| 证书文件 | `continew-webapi/src/main/resources/cert/*` | `/www/wwwroot/continew-admin/cert/` | 微信支付证书 |

---

## ⚠️ 重要提示

### 部署顺序很重要

脚本已经按照正确的顺序上传模块：

1. **continew-common** - 必须先上传（其他模块依赖它）
2. continew-module-system
3. continew-module-education

### 验证部署是否成功

部署后检查以下内容：

#### 1. 检查文件是否上传成功
```bash
ssh tx "ls -lh /www/wwwroot/continew-admin/lib/"
```

应该能看到：
```
continew-common-3.7.0-SNAPSHOT.jar
continew-module-system-3.7.0-SNAPSHOT.jar
continew-module-education-3.7.0-SNAPSHOT.jar
```

#### 2. 检查服务启动日志
```bash
ssh tx "tail -n 100 /www/wwwroot/continew-admin/app.log | grep -i error"
```

不应该再看到 `NoSuchMethodError` 错误。

#### 3. 测试微信登录
```bash
curl -X POST http://student.haoyueke.com.cn/api/mini/auth/oauth/mp \
  -H "Content-Type: application/json" \
  -d '{"code":"test_code"}'
```

---

## 🐛 故障排查

### 问题1：SSH连接失败

**现象：**
```
[ERROR] 无法连接到 tx，请检查 SSH 配置
```

**解决：**
```bash
# 检查SSH配置
cat ~/.ssh/config | grep -A 5 "Host tx"

# 测试连接
ssh tx "echo 'test'"
```

### 问题2：文件上传失败

**现象：**
```
[ERROR] ✗ 上传失败
```

**解决：**
1. 检查远程磁盘空间：`ssh tx "df -h"`
2. 检查远程目录权限：`ssh tx "ls -ld /www/wwwroot/continew-admin"`
3. 手动创建目录：`ssh tx "mkdir -p /www/wwwroot/continew-admin/lib"`

### 问题3：仍然出现 NoSuchMethodError

**可能原因：**
1. ❌ common模块没有上传成功
2. ❌ 服务没有重启
3. ❌ 使用了错误的JAR包

**解决：**
```bash
# 1. 检查远程文件
ssh tx "ls -lh /www/wwwroot/continew-admin/lib/continew-common-3.7.0-SNAPSHOT.jar"

# 2. 检查文件修改时间（应该是最新的）
ssh tx "stat /www/wwwroot/continew-admin/lib/continew-common-3.7.0-SNAPSHOT.jar"

# 3. 强制停止并重启
ssh tx "kill -9 \$(lsof -ti:8080) && cd /www/wwwroot/continew-admin && nohup java -jar bin/continew-admin.jar > app.log 2>&1 &"

# 4. 验证UserContext类
ssh tx "cd /www/wwwroot/continew-admin && jar -tf lib/continew-common-3.7.0-SNAPSHOT.jar | grep UserContext"
```

---

## 📝 修改记录

### 版本 1.1（2026/06/13）
✅ **新增：** continew-common-3.7.0-SNAPSHOT.jar 上传
✅ **修复：** UserContext.setOpenid() 方法缺失问题
✅ **优化：** 调整模块上传顺序，common模块优先

### 版本 1.0
- 初始版本

---

## 🎯 一键部署命令

如果你想一次完成编译、上传、重启，可以使用：

```bash
cd /Users/wangdong/Workspace/Young/continew-admin && \
mvn clean package -DskipTests && \
./deploy.sh && \
ssh tx "cd /www/wwwroot/continew-admin && kill -9 \$(lsof -ti:8080) 2>/dev/null; nohup java -jar bin/continew-admin.jar > app.log 2>&1 &" && \
echo "等待服务启动..." && \
sleep 5 && \
ssh tx "tail -n 50 /www/wwwroot/continew-admin/app.log"
```

---

**文档版本：** v1.1  
**最后更新：** 2026/06/13
